using UnityEngine;

namespace Exprite
{
    public class TextureUtils : MonoBehaviour
    {
        /*
        public float GetPixelsPerUnit(Texture2D texture2D)
        {
            Sprite tempSprite;
            Rect spriteRect = new Rect(0, 0, texture2D.width, texture2D.height);

            Sprite.Create(texture2D, spriteRect, new Vector2(0.5f, 0.5f));
        }
        */
    }
}